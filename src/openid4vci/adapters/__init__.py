"""Credential Format Profiles (Appendix A of the specification).

Each adapter turns issuer-supplied claims into one concrete credential
serialization and declares the metadata fragment that advertises it.
"""
