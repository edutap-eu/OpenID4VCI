"""IETF SD-JWT VC profile, format identifier ``dc+sd-jwt`` (Appendix A.3).

Selective disclosure over a JWT: the credential type is named by ``vct`` and
individual claims can be withheld by the holder at presentation time.

Note the identifier: earlier drafts used ``vc+sd-jwt``. Current profiles --
among them the OpenID4VC High Assurance Interoperability Profile 1.0 -- use
``dc+sd-jwt``.
"""
