"""W3C VCDM profiles (Appendix A.1).

Three variants: ``jwt_vc_json`` (VC as JWT, no JSON-LD),
``jwt_vc_json-ld`` (VC as JWT using JSON-LD) and ``ldp_vc`` (JSON-LD with a
Data Integrity proof, which requires canonicalization).
"""
