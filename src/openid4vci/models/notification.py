"""Notification Endpoint (Section 11).

The Wallet reports back whether a credential actually arrived
(``credential_issued``) or which ``error`` occurred. Optional in the
specification, but it is the only feedback channel an issuer gets, so it is
what makes issuance statistics and re-issuance possible at all.
"""
