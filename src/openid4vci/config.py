"""Configuration via pydantic-settings.

Twelve-factor: everything that differs between deployments -- issuer
identifier, endpoint URLs, key locations, which credential configurations we
offer -- arrives from the environment, never from a checked-in file.
"""
